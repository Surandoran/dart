void main() {
  var name;
  name = '필욱';
  name = 1;
  name = true;

  // dynamic 변수의 타입이 dynamic 즉 정수형이든 문자열이든 뭐든 될 수 있음.
  // dynamic name 으로 선언해 줄 수도 있다.
  // var name 으로도 가능.

  // 응용 name이 String 인가?
  /* if (name is String) {
      name. < 온점을 찍어보면 name이 String인걸 확인 한 후 바이너리에서 명령어를 추천해준다.
  } */
}
